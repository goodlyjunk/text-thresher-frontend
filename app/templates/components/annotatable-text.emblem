#annotatable_text class=reduced:annotatable-text__reduced == formattedText

question-bubble annotator=this bubbleContent=bubbleContent

if reduced
  button.text-toggle click="toggleText" Show full article
else 
  button.text-toggle click="toggleText" Show only partial article


ontology-tree topics=topics
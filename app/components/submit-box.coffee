`import Ember from "ember";`

Component = Ember.Component.extend

  classNames: ["question-bubble"]

  actions:
    submit: ->
      alert("Will implement as soon as we get a POST schema!")

`export default Component;`